# Operations

The project uses a simple three-stage lifecycle:

1. Prepare build metadata with `npm run prepare-build`.
2. Generate the build manifest with `npm run build`.
3. Validate required repository content with `npm test`.

Deployment defaults are maintained in `docs/DEPLOYMENT_GUIDE.md` and should be
included verbatim in generated onboarding or product-wiki summaries.
